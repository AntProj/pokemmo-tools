# Rust build output (large; regenerated by `cargo`/`tauri`)
/target

# Generated icon binaries — run `npm run desktop:icon <path-to-512px.png>`
# to (re)generate these from a source PNG. They are not checked in.
/icons
