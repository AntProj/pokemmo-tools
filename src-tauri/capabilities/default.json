{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the main window, which loads the live site (https://antproj.github.io). `remote` grants IPC to that origin so the app commands (list_windows, capture_and_ocr, flash_toast) and the capture-hotkey event work even though the UI is served remotely.",
  "windows": ["main"],
  "remote": {
    "urls": ["https://antproj.github.io"]
  },
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
